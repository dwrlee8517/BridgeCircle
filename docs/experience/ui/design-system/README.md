# Civic Editorial Design System

The active visual source of truth is [`Civic Design System.html`](Civic%20Design%20System.html).

Prototype source files in [`reference-src/`](reference-src/) are reference material only. They help explain the exported prototype, but they are not production code and should not override the active design-system document or the live app implementation.

When docs and app code disagree, app code reflects current production behavior, but this design-system doc should be updated so downstream design and implementation work has a single trusted reference.

Live implementation anchors:

- [`../../../../app/src/app/globals.css`](../../../../app/src/app/globals.css)
- [`../../../../app/src/components/ui/`](../../../../app/src/components/ui/)
