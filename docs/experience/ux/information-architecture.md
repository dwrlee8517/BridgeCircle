# Information Architecture

Canonical route and navigation architecture lives in [`../../architecture/information-architecture.md`](../../architecture/information-architecture.md).

Use this file as the UX entry point when working inside `docs/experience/`. Do not duplicate route ownership here; update the canonical architecture doc, then revise screen-level notes in [`../screens/`](../screens/) if the IA change affects a screen.
