import { ResetPasswordForm } from './reset-password-form'

export const metadata = { title: 'Reset password · BridgeCircle' }

export default function ResetPasswordPage() {
  return <ResetPasswordForm />
}
