# Expo's Metro bundler and the React Native autolinking scripts resolve native
# modules by walking node_modules directly, which pnpm's default symlinked
# layout breaks. Hoisting is the configuration Expo supports for pnpm.
node-linker=hoisted
