.DS_Store
docs/screenshots/

# Test artifacts from running Playwright from the repository root.
/test-results/
/playwright-report/
/output/

# Personal Obsidian vault — note-taking workspace, not project state.
.obsidian/

# Local design context scratch space, not project state.
claude-design-context/

# Per-user agent tooling. Skills, hooks, and prompts live with the developer,
# not in the repo. If anything in here ever needs to be shared, copy it into
# a tracked path (e.g. app/CLAUDE.md, docs/, a /scripts/ entry) explicitly.
.agent/
.agents/
.codex/
.claude/

# Local scratch space for ad-hoc provider probing (PDL/LinkdAPI/etc).
pdl_test/

# design-sync per-user project pins (each syncer keeps their own; not shared)
.design-sync/config.local.json
**/.design-sync/config.local.json
